function {{ name }}() public {
  {{#each roles}}
    require(participantHasRole(msg.sender, '{{ this }}'), 'You are not allowed to execute this task.');
  {{/each}}

  emit taskExecuted(msg.sender, '{{ name }}');
}
